<?xml version="1.0" encoding="UTF-8"?>
<!--
   Auto-generated using docbuilder.js. Do not edit manually. 
-->
<page xmlns="http://tjansen.de/minifiedPage" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://tjansen.de/minifiedPage ../schemas/page.xsd ">
	<title>.values() - Minified.js</title>
	<section>reference</section>
	<main>
<p><a href="index.html">back to Overview</a></p>
<h3><a name="doc-values">.values()</a></h3>
<div class="summary">Creates a name/value map from the given form.</div>

<h4>Syntax</h4>
<div class="syntaxVariant">
<div class="syntax">MINI().values()</div>
<div class="syntax">MINI().values(dataMap)</div>
</div>

<h4>Parameters</h4>
<dl class="params">
<dt id="values_dataMap"><a name="values_dataMap">dataMap</a></dt>
<dd><span class="optional">optional</span> an optional map to write the values into. If not given, a new empty map will be created</dd>
<dt id="values_RETURN" class="returnValue"><a name="values_RETURN">return value</a></dt>
<dd>a map containing name->value pairs as strings. If there is more than one value with the same name,
map value is an array of strings</dd>
</dl>

<h4>Description</h4>
<div class="description">Creates a name/value map from the given form. values() looks at the list's form elements and writes each element's name into the map,
using the element name as key and the element's value as value. If there is more than one value with the same name, the map will contain an array
of values. Form element without value will be written with 'null' as value. Form elements without name will be ignored.

values() will use all elements in the list that have a name, such as input, textarea and select elements. For form elements in the list, all child form
elements will be serialized.

The map format returned by values() is exactly the format used by request().

Please note that when you include an input element more than once, for example by having the input itself and its form in the list, the
value will be included twice in the list.

</div>

<div class="examples">
<h4>Example</h4>
<div class="example">Serialize a form and send it as request parameters:
<pre>
MINI.request('get', '/exampleService', $('#myForm').values(), resultHandler);
</pre>
</div>
<h4>Example</h4>
<div class="example">Serialize only some selected input fields:
<pre>
var data = $('#myText, input.myRadios').values();
</pre>
</div>
</div>


<p><a href="index.html">back to Overview</a></p>
	</main>
</page>
